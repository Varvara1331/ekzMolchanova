Найденные запахи кода:
Нарушение приниципа инкапсуляции (ООП): глобальная переменная GlobalN.n
Для решения необходимо измненение Floyd.cs, что запрещено условиями

Дублирование логики инициализации матрицы:
for (int i = 0; i < n; i++)
    for (int j = 0; j < n; j++)
        adjMatrix[i, j] = double.PositiveInfinity;

for (int i = 0; i < n; i++)
    adjMatrix[i, i] = 0;
Можно создать дополнительный метод для избежания дублирования